# Synthetic benchmark

Use `python -m agenticbo benchmark` from the repository root.
`run.py` and `run.sh` are compatibility entry points to the same implementation.

See the [main README](../../README.md) for setup, smoke commands, initialization,
paper differences, and outputs. Configuration is in
[configs/benchmark.yaml](../../configs/benchmark.yaml).
